/**
 * EngagementScript — REMOVED
 *
 * Previously contained hidden iframe pixel, auto-redirect, and impression beacon.
 * All removed to comply with Google Ads policies (no hidden elements,
 * no silent tracking, no auto-redirects).
 *
 * This file is kept as a no-op stub so existing imports don't break.
 */
export function EngagementScript(_props: {
  offerSlug?: string;
  cluster?: string;
  redirectDelaySec?: number | null;
}) {
  return null;
}
